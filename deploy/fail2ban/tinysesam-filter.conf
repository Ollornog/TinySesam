# /etc/fail2ban/filter.d/tinysesam.conf
# Matcht die von TinySesam geloggten Fehlversuche (Logger "tinysesam.security").
# Die App muss diesen Logger auf eine Datei/journald schreiben, das die Jail liest.
[Definition]
failregex = failed login user=.* ip=<HOST> method=.*
ignoreregex =
